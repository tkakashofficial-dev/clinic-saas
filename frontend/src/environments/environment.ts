// Production configuration. The API URL is same-origin ('/api') — in production
// the frontend is served behind a reverse proxy / static host with API routing.
export const environment = {
  production: true,
  apiUrl: '/api',
};
